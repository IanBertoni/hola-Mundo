(function (a, b) {
  console.log(`Ejecutando la suma de la funcion anonima: ${a + b} `);
})(3, 5); //con estos parentesis se llama la funcion asi misma.
// en quokka se ve como se ejecuta pero este tipo de funciones no se las puede reutilizar porque se llaman asi misma una sola vez(a parte de ni siquiera tener nombre),
