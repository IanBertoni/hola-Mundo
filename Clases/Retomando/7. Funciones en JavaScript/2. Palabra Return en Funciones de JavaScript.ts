console.log(miFuncion(4, 2));

//Declaro la funcion
function miFuncion(a: number, b: number) {
  return a + b;
}

// Puedo llamar a la funcion en donde yo quiera una vez la tenga declarada

let resultado = miFuncion(42, 22);

console.log(resultado);
